# Overwrite the netlify.toml file with the corrected version
echo '[build]
  base = "Client"
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"' > netlify.toml

# Verify the change
Get-Content netlify.toml